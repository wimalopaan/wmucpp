ToDo
============
* CRSF relay: select source (RX forward, analog, bt)
* centralize all inputs as source for outputs (hwext, sbus, relay)
* wait for bind of receiver max. 60s (link-stats package)
  send test packages,
  if these packages are received, send receiver enter bind more command
* Bluetooth: bind command for RX
* CPPM-in analog calibration
* analog calibration
* integrate GPS (UART)
* integrate compass (QMC5833L)
* intergate accelerometer (MPU6050, MPU6250)
* deliver (modified) GPS data (ublox proto) with tilt-compensated compass data injected

LUA Widget
============

Done
============
* CRSF half-duplex relay
* CRSF relay mode
* CRSF address rewrite for RX-as-TX
* ELRS-client mode
* Prop values
* prop values alternating to switch values
* Master-Mode (Controller number == 0)
* Display prop values
* dialog to add prop mapping
