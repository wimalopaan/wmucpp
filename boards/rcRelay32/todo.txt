Hardware:
=========

* V-10: PCB for JR-Bay-Modules
* V-20: PCB for serial rx-as-tx

Bugs:
=====

* Telemetry-Tunnel LUA: stopped working on EdgeTx 3.0?

ToDo:
=====
* watchdog (show watchdog counter in menu)
* adc input: voltage, temperature
* EByte E32-170T30D relay mode

Done:
=====
* check 1,8MBaud with TBS tracer (not not work)
* menu: option for baudrate selection (400/420/460/921/1,8)
* correct docu (RX and TX interchanged, A11 for half-duplex TX module)
* allow more baudrates (e.g. 115200Bd for IR-Connection)
* option to select IrDa-SIR mode
* adapt to own stm32g0b1 board
* make LUA-widget to grab tunneled telemetry
* refactor packet_relay_rewrite
* enable telemetry tunnel type specific -> menu options for all types of sensors
* explicit failsafe mode (menu: hold, forward, zero, ...)
* failsafe: switch off relay if no input anymore
* blink pattern for no-input signal (single flash)
* telemetry tunnel for telemetry messages
** maybe too much load? store/forward using slower rate?
* move update-from-eeprom to later state, and move crsf/relay init to early state
* add half-duplex relay mode
* configure relay telemetry (actual all except link stats) is send
* encapsulte secondary link stat packages
